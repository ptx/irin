#include "gtest/gtest.h"
//#include "server/http-message-decoder.h"

TEST(Foo, Bar) {
  //std::unique_ptr<HttpMessageDecoder> decoder(new HttpMessageDecoder(std::vector<char>()));
  //int result = decoder->AddFive(5);
  //ASSERT_EQ(result, 9);
};
